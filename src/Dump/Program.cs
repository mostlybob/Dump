namespace Display;

public class Output
{
    static void Main()
    {

    }
    /*
    Ultimately, I could see this as some sort of extension method on Object,
    similar to how LINQPad does it, but as this will be dumping to a console
    instead of the rich text box that LP uses, this will just return a string,
    hopefully with a bit of formatting to make it easier to read
    */
    public string Dump(object forDisplay)
    {
        return null;
    }
}